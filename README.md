# PortControl

## Current documentation

Start with `docs/current/START_HERE_CURRENT_v1.9.md`. The original root `*_FINAL_v1.5` documents are retained as legacy/frozen handoff history; current Core semantics live under `docs/current/`.

## Integrity

`DELIVERY_CHECKSUMS.sha256` covers current `src/`, `test/`, `docs/`, `scripts/` and root runtime/config files. Verify with `npm run verify:delivery`. `PACKAGE_CHECKSUMS.sha256` remains the unchanged legacy v1.5 package checksum.
